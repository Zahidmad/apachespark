apiVersion: v1
kind: Pod
metadata:
  name: kaniko
  namespace: spark
spec:
  containers:
  - name: kaniko
    image: gcr.io/kaniko-project/executor:latest
    args:
    - "--context=git://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@github.com/Zahidmad/apachespark.git"
    - "--destination=zramazanli/spark-demo-image:2.0"
    - "--dockerfile=kubernetes/dockerfiles/spark/Dockerfile"
    volumeMounts:
    - name: kaniko-secret
      mountPath: /kaniko/.docker
  restartPolicy: Never
  volumes:
  - name: kaniko-secret
    secret:
      secretName: dockercred
      items:
        - key: .dockerconfigjson
          path: config.json
