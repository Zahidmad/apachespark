apiVersion: v1
kind: Pod
metadata:
  name: kaniko
  namespace: spark
spec:
  containers:
  - name: kaniko
    image: gcr.io/kaniko-project/executor:latest
    args:
    - "--context=git://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@github.com/Zahidmad/gitops.git"
    - "--destination=zramazanli/spark-demo-image:2.0"
    - "--dockerfile=kubernetes/dockerfiles/spark/Dockerfile"
    volumeMounts:
    - name: kaniko-secret
      mountPath: /kaniko/.docker
  restartPolicy: Never
  volumes:
  - name: kaniko-secret
    secret:
      secretName: dockercred
      items:
        - key: .dockerconfigjson
          path: config.json
